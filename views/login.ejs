<!doctype html>
<html>
<head><title>LeadBoost — Login</title></head>
<body>
  <h2>Login</h2>
  <% if (messages.error) { %><div style="color:red"><%= messages.error %></div><% } %>
  <% if (messages.success) { %><div style="color:green"><%= messages.success %></div><% } %>
  <form method="post" action="/login">
    <input name="email" placeholder="Email" required /><br/>
    <input name="password" type="password" placeholder="Password" required /><br/>
    <button>Login</button>
  </form>
</body>
</html>
